
CREATE TABLE IF NOT EXISTS DEPARTMENTS (
    dept_code INTEGER PRIMARY KEY,
    dept_name VARCHAR(50)
);


-- INSERT INTO DEPARTMENTS (dept_code, dept_name) VALUES (5, 'CSE');
-- INSERT INTO DEPARTMENTS (dept_code, dept_name) VALUES (6, 'EEE');
-- INSERT INTO DEPARTMENTS (dept_code, dept_name) VALUES (1, 'ARCHITECHTURE');


CREATE TABLE IF NOT EXISTS Student (
    student_id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password TEXT NOT NULL,
    department VARCHAR(50) NOT NULL,
    reputation_points INT DEFAULT 0
);


CREATE TABLE IF NOT EXISTS Admin (
    admin_id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password TEXT NOT NULL,
    department VARCHAR(50) NOT NULL
);


CREATE TABLE IF NOT EXISTS TAGS (
    tag_id SERIAL PRIMARY KEY,
    tag_title VARCHAR(20) UNIQUE
);



CREATE TABLE IF NOT EXISTS DEPT_GROUPS (
    group_id SERIAL PRIMARY KEY,
    dept_code INTEGER UNIQUE REFERENCES DEPARTMENTS(dept_code),
    group_name VARCHAR(50)
);


CREATE TABLE IF NOT EXISTS COURSES (
    course_code CHAR(7) PRIMARY KEY,
    course_title VARCHAR(50),
    dept_code INTEGER REFERENCES DEPARTMENTS(dept_code)
);


CREATE TABLE IF NOT EXISTS Resources (
    resource_id SERIAL PRIMARY KEY,
    title VARCHAR(150) NOT NULL,
    description TEXT,
    file_url TEXT NOT NULL,                  -- PDF/Drive link/File path
    file_type VARCHAR(20) NOT NULL,          -- 'PDF', 'Image', 'Link'
    uploaded_by INT REFERENCES Student(student_id) ON DELETE CASCADE,
    dept_code INT REFERENCES DEPARTMENTS(dept_code) ON DELETE CASCADE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


CREATE TABLE IF NOT EXISTS DOUBTS (
    doubt_id SERIAL PRIMARY KEY,
    title VARCHAR(100),
    description VARCHAR(500),
    course_code CHAR(7) REFERENCES COURSES(course_code),
    author INTEGER REFERENCES Student(student_id),
    posted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    solved BOOL DEFAULT FALSE
);


CREATE TABLE IF NOT EXISTS DOUBT_TAGS (
    doubt_id SERIAL REFERENCES DOUBTS(doubt_id),
    tag_id INTEGER REFERENCES TAGS(tag_id),
    PRIMARY KEY (doubt_id, tag_id)
);

CREATE TABLE IF NOT EXISTS RESOURCE_COMMENTS (
    comment_id SERIAL PRIMARY KEY,
    comment_text VARCHAR(400),
    resource_id INTEGER REFERENCES Resources(resource_id) ON DELETE CASCADE,
    author INTEGER REFERENCES Student(student_id),
    commented_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


CREATE TABLE IF NOT EXISTS ANSWERS (
    answer_id SERIAL PRIMARY KEY,
    doubt_id INTEGER REFERENCES DOUBTS(doubt_id) ON DELETE CASCADE,
    answer_text VARCHAR(400),
    author INTEGER REFERENCES Student(student_id),
    answered_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS BOOKMARKS (
    student_id INT REFERENCES Student(student_id) ON DELETE CASCADE,
    resource_id INT REFERENCES Resources(resource_id) ON DELETE CASCADE,
    bookmarked_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (student_id, resource_id)
);


CREATE TABLE IF NOT EXISTS  VOTES (
    vote_id SERIAL PRIMARY KEY,
    student_id INT REFERENCES Student(student_id) ON DELETE CASCADE,
    resource_id INT REFERENCES Resources(resource_id) ON DELETE CASCADE,
    vote_type VARCHAR(10) NOT NULL, -- 'UP' or 'DOWN'
    voted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT unique_user_resource_vote UNIQUE (student_id, resource_id)
);


CREATE TABLE IF NOT EXISTS REPORTS (
    report_id SERIAL PRIMARY KEY,
    student_id INT REFERENCES Student(student_id) ON DELETE CASCADE,
    resource_id INT REFERENCES Resources(resource_id) ON DELETE CASCADE,
    reason TEXT NOT NULL,
    reported_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);



CREATE TABLE IF NOT EXISTS JOINED_GROUPS(
student_id INTEGER REFERENCES STUDENT(student_id),
group_id INTEGER REFERENCES DEPT_GROUPS(group_id)
)

--MADE GROUP ID AUTO GENERATED
ALTER TABLE dept_groups
ALTER COLUMN group_id ADD GENERATED BY DEFAULT AS IDENTITY;

--ALREADY ADDED CSE,EEE,ARCHI



INSERT INTO DEPT_GROUPS(DEPT_CODE,GROUP_NAME) VALUES(05,'CSE');
INSERT INTO DEPT_GROUPS(DEPT_CODE,GROUP_NAME) VALUES(06,'EEE');
INSERT INTO DEPT_GROUPS(DEPT_CODE,GROUP_NAME) VALUES(01,'ARCHITECTURE');
INSERT INTO DEPT_GROUPS(DEPT_CODE,GROUP_NAME) VALUES(07,'CIVIL');


INSERT INTO DEPARTMENTS VALUES(05,'CSE');
INSERT INTO DEPARTMENTS VALUES(06,'EEE');
INSERT INTO DEPARTMENTS VALUES(01,'ARCHITECTURE');
INSERT INTO DEPARTMENTS VALUES(07,'CIVIL');
INSERT INTO DEPARTMENTS VALUES(08,'MECHANICAL');


INSERT INTO STUDENT VALUES(2405157,'TAMANNA HAQUE OYESHI','oyeshi1308@gmail.com','12345678','CSE',0);
INSERT INTO STUDENT VALUES(2405156,'Tasnim Haque Toshi','hello@gmail.com','12345678','CSE',0);
INSERT INTO STUDENT VALUES(2405100,'Fabiha Ishrah','oyeshi1308@gmail.com','12345678','CSE',0);



SELECT *
FROM DEPT_GROUPS DG JOIN STUDENT S
ON DG.GROUP_NAME = S.DEPARTMENT
WHERE S.STUDENT_ID = 2405157;


INSERT INTO COURSES VALUES(101,'STRUCTURED PROGRAMMING',05),
(102,'STRUCTURED PROGRAMMING SESSIONAL',05),
(163,'ELECTRONICS BASICS',06),
(164,'ELECTRONICS BASICS SESSIONAL',06),
(205,'DIGITAL LOGIC DESIGN',05),
(206,'DIGITAL LOGIC DESIGN SESSIONAL',05),
(103,'ENGINEERING BASICS',07);